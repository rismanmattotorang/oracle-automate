apiVersion: v1
kind: Service
metadata:
  name: oracle-automate-server
  namespace: oracle-automate
  labels:
    app.kubernetes.io/name: oracle-automate-server
    app.kubernetes.io/part-of: oracle-automate
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: oracle-automate-server
  ports:
    - name: http
      port: 3030
      targetPort: http
      protocol: TCP
  # Sticky sessions are important — the paper §IV-K reference deployment
  # uses consistent hashing on `mcp-session-id`.  ClusterIP supports
  # `sessionAffinity: ClientIP` as a coarse first step; the real
  # session-aware routing lives in the Envoy/Istio sidecar config (see
  # `helm/oracle-automate/templates/envoyfilter.yaml` for an example).
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
